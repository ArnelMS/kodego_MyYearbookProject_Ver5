<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="400dp"
    android:layout_height="150dp">

    <TextView
        android:id="@+id/etItemDescription"
        android:layout_width="196dp"
        android:layout_height="78dp"
        android:layout_marginTop="8dp"
        android:maxLines="1"
        android:text="Item Descriptions here...."
        android:textAlignment="textStart"
        android:textColor="@color/black"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@+id/etItemTitle"
        app:layout_constraintTop_toBottomOf="@+id/etItemTitle" />

    <TextView
        android:id="@+id/etItemTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:maxLines="1"
        android:text="Sample Event Title"
        android:textColor="@color/black"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@+id/imgDashboardNewItem"
        app:layout_constraintTop_toTopOf="@+id/imgDashboardNewItem" />

    <ImageView
        android:id="@+id/imgDashboardNewItem"
        android:layout_width="160dp"
        android:layout_height="109dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:scaleType="fitXY"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@tools:sample/backgrounds/scenic" />

    <!--        android:src="@drawable/mainbg"-->
    <!--        app:shapeAppearance="@style/ShapeAppearanceOverlay.Avatar" />-->


</androidx.constraintlayout.widget.ConstraintLayout>